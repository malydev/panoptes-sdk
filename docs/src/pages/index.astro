---
/**
 * Home Page
 * Uses composition to build page from smaller components
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/content/Hero.astro';
import Card from '../components/ui/Card.astro';
import CodeExample from '../components/content/CodeExample.astro';
import Button from '../components/ui/Button.astro';
---

<BaseLayout
  title="Panoptes SDK - Enterprise SQL Auditing"
  description="Automatically audit all SQL queries with rich context, flexible rules, and zero configuration">

  <!-- Hero Section -->
  <Hero
    title="Panoptes SDK"
    tagline="Enterprise-grade SQL auditing for modern applications. A better alternative to database triggers."
  />

  <!-- Features Section -->
  <section class="py-16 px-6">
    <div class="max-w-7xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-12 text-slate-900 dark:text-white">
        Why Panoptes?
      </h2>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        <Card title="⚡ Zero Configuration" icon="⚡">
          <p>Auto-creates audit tables. No manual setup, no SQL scripts. Just install and start auditing.</p>
        </Card>

        <Card title="🗄️ Multi-Database" icon="🗄️">
          <p>PostgreSQL, MySQL, MSSQL, SQLite, and Oracle. One SDK, all databases.</p>
        </Card>

        <Card title="👤 Rich Context" icon="👤">
          <p>Track who, what, when, and where for every query with full application context.</p>
        </Card>

        <Card title="🎯 Flexible Rules" icon="🎯">
          <p>Configure auditing per table or operation type with powerful rule engine.</p>
        </Card>

        <Card title="📊 Multiple Transports" icon="📊">
          <p>Send events to Console, File, HTTP endpoints, or separate databases.</p>
        </Card>

        <Card title="⚙️ Production Ready" icon="⚙️">
          <p>Battle-tested, optimized, and secure. Used in production worldwide.</p>
        </Card>
      </div>
    </div>
  </section>

  <!-- Quick Start Section -->
  <section id="getting-started" class="py-16 px-6 bg-white/50 dark:bg-slate-900/50">
    <div class="max-w-5xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-12 text-slate-900 dark:text-white">
        Quick Start
      </h2>

      <CodeExample title="1. Install" language="bash">
npm install @panoptes/sdk
      </CodeExample>

      <CodeExample title="2. Initialize & Wrap Client" language="javascript">
import { initAudit, createAuditedPostgresClient } from '@panoptes/sdk';
import { Client } from 'pg';

// Initialize Panoptes
initAudit({
  appName: 'my-app',
  transports: {
    enabled: ['database'],
    database: {
      client: auditDbClient,
      engine: 'postgres',
      autoCreateTable: true  // ⭐ Auto-creates audit table
    }
  }
});

// Wrap your database client
const client = new Client({ /* config */ });
const auditedClient = createAuditedPostgresClient(client, {
  host: 'localhost',
  database: 'mydb'
});

// All queries are now automatically audited! ✅
await auditedClient.query('SELECT * FROM users WHERE id = $1', [123]);
      </CodeExample>

      <div class="text-center mt-12">
        <Button href="#docs" variant="primary" size="lg">
          Read Full Documentation
        </Button>
      </div>
    </div>
  </section>

  <!-- Comparison Section -->
  <section class="py-16 px-6">
    <div class="max-w-5xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-12 text-slate-900 dark:text-white">
        Better Than Database Triggers
      </h2>

      <div class="bg-white dark:bg-slate-800 rounded-xl shadow-xl overflow-hidden">
        <table class="w-full">
          <thead class="bg-gradient-to-r from-blue-600 to-purple-600 text-white">
            <tr>
              <th class="px-6 py-4 text-left">Feature</th>
              <th class="px-6 py-4 text-center">Triggers ❌</th>
              <th class="px-6 py-4 text-center">Panoptes ✅</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-slate-200 dark:divide-slate-700">
            <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/50">
              <td class="px-6 py-4 font-medium">Portable</td>
              <td class="px-6 py-4 text-center text-red-600">❌</td>
              <td class="px-6 py-4 text-center text-green-600">✅</td>
            </tr>
            <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/50">
              <td class="px-6 py-4 font-medium">Rich App Context</td>
              <td class="px-6 py-4 text-center text-red-600">❌</td>
              <td class="px-6 py-4 text-center text-green-600">✅</td>
            </tr>
            <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/50">
              <td class="px-6 py-4 font-medium">Multiple Destinations</td>
              <td class="px-6 py-4 text-center text-red-600">❌</td>
              <td class="px-6 py-4 text-center text-green-600">✅</td>
            </tr>
            <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/50">
              <td class="px-6 py-4 font-medium">Version Controlled</td>
              <td class="px-6 py-4 text-center text-red-600">❌</td>
              <td class="px-6 py-4 text-center text-green-600">✅</td>
            </tr>
            <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/50">
              <td class="px-6 py-4 font-medium">Non-blocking</td>
              <td class="px-6 py-4 text-center text-orange-600">⚠️</td>
              <td class="px-6 py-4 text-center text-green-600">✅</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-12 px-6 border-t border-slate-200 dark:border-slate-700">
    <div class="max-w-7xl mx-auto text-center text-slate-600 dark:text-slate-400">
      <p class="text-lg mb-2">Created by <a href="https://github.com/malydev" class="font-semibold text-blue-600 hover:text-blue-700">malydev</a> (Miguel Lipa)</p>
      <p class="text-sm">MIT License • Built with ❤️ for the developer community</p>
    </div>
  </footer>
</BaseLayout>
